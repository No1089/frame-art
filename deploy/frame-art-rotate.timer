[Unit]
Description=Check the Frame TV every minute, change the art every few

[Timer]
# Frequent and cheap: it does nothing at all unless the TV is showing art.
OnCalendar=*:*:00
Persistent=false

[Install]
WantedBy=timers.target
